// TODO(phase-06): Implement EventChannel bridge between Kotlin KavachSentinelService and Flutter.
// Channel: 'com.digitalkavach.sentinel/events'
// Handle cold/warm start, background isolate, stream subscription.
// Persist flagged items into Hive feed + trigger Tier-2 via WorkManager.

/// Sentinel bridge — implemented in Phase 06.
library;